# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FakeCall' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
pod 'Firebase/AdMob'
pod 'Firebase/Core'


  # Pods for FakeCall

  target 'FakeCallTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FakeCallUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
